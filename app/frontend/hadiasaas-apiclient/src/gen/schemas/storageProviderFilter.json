{
    "type": "object",
    "properties": {
        "equals": {
            "type": "string",
            "enum": ["LOCAL", "AWS_S3", "AZURE_BLOB", "GCS"]
        },
        "notEquals": {
            "type": "string",
            "enum": ["LOCAL", "AWS_S3", "AZURE_BLOB", "GCS"]
        },
        "specified": { "type": "boolean" },
        "in": {
            "type": "array",
            "items": {
                "type": "string",
                "enum": ["LOCAL", "AWS_S3", "AZURE_BLOB", "GCS"]
            }
        },
        "notIn": {
            "type": "array",
            "items": {
                "type": "string",
                "enum": ["LOCAL", "AWS_S3", "AZURE_BLOB", "GCS"]
            }
        }
    },
    "x-readme-ref-name": "StorageProviderFilter"
}
