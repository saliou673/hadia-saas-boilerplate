{
    "type": "object",
    "properties": {
        "planId": { "type": "integer", "format": "int64" },
        "billingFrequency": {
            "type": "string",
            "enum": ["MONTHLY", "YEARLY", "LIFETIME", "CUSTOM"]
        },
        "discountCode": { "type": "string", "maxLength": 50, "minLength": 0 }
    },
    "required": ["billingFrequency", "planId"],
    "x-readme-ref-name": "StripePaymentIntentRequest"
}
